spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pc_parts
    username: adam
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
